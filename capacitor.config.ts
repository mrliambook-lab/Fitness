import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tendaybellyfat.app',
  appName: 'TenDayBellyFat',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
